/*
    CSS Grid generated by Outline - {{& url }}

    Width   - {{ width }}
    Columns - {{ columns }}
    Margin  - {{ margin }}
*/

/*
    Span classes specify how many columns the element should extend over.

        <div class='span-6'>...</div>
*/
{{# spans }}
{{ cssClass }}{{ separator }}{{/ spans }} {
    margin-right: {{ margin }};
    float: left;
    display: inline;
}

{{# spans }}
{{ cssClass }} { width: {{ width }}; }
{{/ spans }}

/*
    Prefix classes specify how many empty columns there should be before the element.

        <div class='span-4 prefix-2'>...</div>
*/
{{# prefixes }}
{{ cssClass }} { padding-left: {{ padding }}; }
{{/ prefixes}}

/*
    Suffix classes specify how many empty columns there should be after the element.

        <div class='span-4 suffix-2'>...</div>
*/
{{# suffixes }}
{{ cssClass }} { padding-right: {{ padding }}; }
{{/ suffixes}}

/*
    The container is used to set your grid width and center your columns.

        <div class='container'>
            <div class='span-5 prefix-1 suffix-1'>...</div>
        </div>
*/
.container {
    margin-left: auto;
    margin-right: auto;
    width: {{ width }};
}

/*
    You can apply the overlay class to a row or grid element to see where the columns lie.
    This is very helpful when debugging a layout.

        <div class='row grid-overlay'>
            <div class='span-7'>...</div>
        </div>
*/
.grid-overlay {
    background: url("{{& overlay }}") left top repeat;
}

/*
    The last class removes the right margin of the element that is applied with the span classes.
    You must add this to your last element in each row.
    An element which spans all columns will have it automatically applied.

        <div class='span-6'>...</div>
        <div class='span-6 last'>...</div>
*/
.last, {{ lastSpan.cssClass }} {
    margin-right: 0;
}

/*
    Rows are used in place of the traditional clearfix line break.
    Just wrap your rows of elements in a row div to wrap the row early without having to go to the end.

        <div class='row'>
            <div class='span-6 prefix-6 last'>...</div>
        </div>

        <div class='row'>
            <div class='span-6'>...</div>
        </div>
*/
.row {
    clear: both;
    display: block;
    float: left;
    width: 100%;
}